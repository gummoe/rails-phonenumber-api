Super simple phone-number API built in Ruby on Rails. 
Supports:
Loading in a CSV of phone number data with the following structure: Number, Context, Name
* GET for a known record
* POST for a new record
